'use strict';

const path = require('path');

const PATHS = {
  src: path.resolve(__dirname, '../llm-translator-chrome-extention'),
  build: path.resolve(__dirname, '../build/llm-translator-chrome-extention'),
};

module.exports = PATHS;
